# mnemo v4.4 — Nebula Renderer Swap (sigma.js v3) Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: superpowers:executing-plans. RIGID: superpowers:test-driven-development, superpowers:verification-before-completion. Live-verify per `feedback_reproduce_user_exact_scenario` at an EXPLICIT viewport (0-height-vp artifact, gotcha 19). **TOMBSTONE (gotcha 31 / `reference_cosmos_gl_nebula`):** never re-attempt cosmos.gl — v4.4 *replaces* it; if a phase regresses, do NOT fall back to cosmos config-tuning.

**Goal:** Replace `@cosmos.gl/graph` with **sigma.js v3 + graphology** so the Nebula graph delivers real node-highlight + full interaction (click/hover/drag/camera), themed entirely from the C1 tokens + `palette.py`, closing the gotcha-31 / C3-honesty loop.

**Architecture:** Renderer-layer swap only. Reuse verbatim: the `{% block layout %}`+`<main class="full">`+`.nebula-shell` 3-panel shell, `/ui/graph-data` + the server layout cache (`_graph_scope_key`/`_graph_fingerprint`) + workspace scoping. Vendor sigma+graphology (no Node build). Highlight via sigma `nodeReducer`; the companion `mnemo_highlight_nodes` `_ui` sentinel drives it.

**Tech Stack:** sigma.js v3 + graphology (vendored UMD/ESM in `daemon/mnemo/ui/static/vendor/`), Alpine.js (`nebula()`), Jinja2, pytest grep-guard tests, preview tool.

---

## Context & prerequisites

- **Branch:** `release/4.4.0` off `main` **after v4.3.2 merges** (`git checkout main && git pull && git checkout -b release/4.4.0`).
- **Validated design:** `docs/plans/2026-05-17-mnemo-v4.4-nebula-renderer-swap-design.md` (read it).
- **Reading list:** `reference_cosmos_gl_nebula.md` (the ceiling being escaped — TOMBSTONE), `reference_mnemo_v3.md` g19/g31, `feedback_cytoscape_minified_core` (empty-canvas deselect capability-test), `feedback_single_source_palette` (palette contract), `feedback_reproduce_user_exact_scenario`, `reference_mnemo_pipelines` §4/§13.
- **Hard rules:** conventional commits; **no Co-Authored-By**; no emojis; HEREDOC; run from `daemon/`; git from repo root.
- **Ground-truth (verified):** renderer import `graph.html:16-24`; container `<div id="cy-nebula">` ~`:125`; `x-data="nebula()"` `:35`; data fetch `/ui/graph-data` `:639`; layout cache `routes.py:58-74`; graph route `routes.py:237`; C1 tokens `app.css:13-15,34,38`; palette `window.MNEMO_TYPE_COLORS` (base.html:60-61); highlight tool `agent_tools.py:854` `_mnemo_highlight_nodes` → `_ui("highlight_nodes",...)`; C3 honesty wording `agent_tools.py:455/471/856`.

---

## Task 1: Vendor sigma+graphology + TOMBSTONE guard test

**Files:** Create `daemon/mnemo/ui/static/vendor/sigma.min.js`, `graphology.umd.min.js`, `graphology-layout-forceatlas2.min.js` (pinned versions, vendored — no CDN runtime dep). Test: `daemon/tests/unit/test_nebula_renderer.py`.

1. **Failing test** — assert `graph.html` (a) does NOT contain `@cosmos.gl`, (b) loads `sigma` + `graphology` from `/static/vendor/`, (c) keeps `cy-nebula` + `.nebula-shell` tokens (shell unchanged), (d) no `setConfig(`/`setPointPositions(` cosmos tokens. Run → RED.
2. Vendor the libs; swap `graph.html:16-24` import. Run → GREEN.
3. `cd daemon && uv run pytest tests/unit/test_nebula_renderer.py -q && uv run ruff check .`
4. Commit `feat(ui): v4.4 -- vendor sigma.js+graphology, drop cosmos.gl (TOMBSTONE)`.

## Task 2: Render the graph (static, C1-themed) at parity

**Files:** Modify `graph.html` (`nebula()` Alpine: build a graphology Graph from `/ui/graph-data`, instantiate `Sigma` on `#cy-nebula`); reuse the data fetch.

1. Test: `/graph` 200 + body has `new Sigma(`/`graphology` + reads `window.MNEMO_TYPE_COLORS` (palette contract) — RED.
2. Implement: nodes colored from `MNEMO_TYPE_COLORS[type]`, bg `--bg` starfield kept, labels native (delete the DOM label overlay), positions from the server layout cache. GREEN.
3. Live-verify (explicit 1280x900): node count rendered == scope count; screenshot-independent geometry probe; ≥15s no freeze.
4. Full suite + ruff; commit `feat(ui): v4.4 -- sigma static render at parity, themed from C1 palette`.

## Task 3: Layout (reuse cache; bounded forceatlas2 fallback; freeze)

1. Test: when `/ui/graph-data` returns cached positions, no layout runs (assert the worker isn't started); when absent, forceatlas2 runs ≤ N iterations then `kill()` (no perpetual sim — the cosmos lesson). RED→impl→GREEN.
2. Live-verify: positions stable, no never-cool CPU; commit.

## Task 4: Full interaction

**Files:** `graph.html` `nebula()` — sigma `clickNode`/`enterNode`/`leaveNode`/`downNode`+drag, `camera.animate`, click-empty deselect (capability-test, not identity-compare — `feedback_cytoscape_minified_core`).

1. Tests (grep + behavior): `clickNode` → select + `$dispatch('mnemo-cite')`; empty-click → deselect; camera center on select. RED→impl→GREEN.
2. Live-verify each on a real conversation/scope (probes, not screenshots); commit `feat(ui): v4.4 -- full graph interaction (click/hover/drag/camera/deselect)`.

## Task 5: Real highlight + close the C3-honesty loop

**Files:** `graph.html` (`highlight(nodeIds)` Alpine + sigma `nodeReducer`/`edgeReducer`: in-set full `--accent` glow, rest dimmed alpha ~0.15, never hidden); wire the `mnemo_highlight_nodes` `_ui("highlight_nodes")` action → `highlight()`; **revert** the C3 "side panel, not the graph" wording in `agent_tools.py:455/471/856` to the now-true "highlighted in the graph".

1. Tests: `nodeReducer` + `highlight(` exist; the `_ui` highlight action is wired in `nebula()`; `agent_tools.py` no longer says "do NOT tell the user it lit the graph" (now it DOES). Update `test_nebula_highlight_honesty.py` to the v4.4 truth (graph highlight is real now) — contract-evolution, not weakening. RED→impl→GREEN.
2. Live-verify: `highlight(['<id>',...])` → computed alpha of out-set ≈0.15, in-set ≈1 + accent; the companion tool path drives it end-to-end.
3. Full suite + ruff; commit `feat(ui+agent): v4.4 -- real graph node-highlight; C3 wording reverted to true`.

## Task 6: Beauty/polish pass + full live-verify

Glow, hover halo, selection ring, edge fade, starfield, transitions — tuned to the dark C1 theme ("most beautiful" bar). Live-verify the FULL §Testing matrix from the design (real viewport): render parity, highlight dims+spotlights, click/cite, camera, drag, ≥15s no-freeze. Commit `feat(ui): v4.4 -- Nebula polish (glow/halo/transitions, C1 dark theme)`.

## Task 7: Release v4.4.0

`reference_mnemo_pipelines` §4+§13: stop preview daemon → `cd daemon && uv run pytest -q && uv run ruff check . && uv run ruff format --check .` → bump `daemon/pyproject.toml`+`daemon/mnemo/__init__.py`+`CHANGELOG.md (## [4.4.0])`+`uv lock` → `chore(release): v4.4.0` → `git tag -a v4.4.0` → restart daemon, `/v1/health`=4.4.0 → push branch+tag → `gh pr create` → watch CI → merge → release.yml publishes → `session_handover_v4_4_shipped.md` + `MEMORY.md` + final reindex (daemon kept UP, foreground, long timeout — the proven §13 pattern).

---

## Done criteria

- [ ] cosmos.gl GONE; sigma.js v3 + graphology vendored + rendering; TOMBSTONE guard test green.
- [ ] Real node-highlight (reducer); the companion `mnemo_highlight_nodes` lights the graph; C3 "side panel" wording reverted to the now-true "graph".
- [ ] Full interaction (click/hover/drag/camera/deselect); 3-panel shell + `/ui/graph-data` + scope/layout-cache unchanged.
- [ ] Themed 100% from C1 tokens + `palette.py`; polished to the dark-theme beauty bar; ≥15s no-freeze at a real viewport.
- [ ] Full suite + ruff green; v4.4.0 tagged, PR merged, published, handover + reindex done.
