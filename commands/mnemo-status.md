---
description: Show mnemo daemon status, node count, and source list
---

```bash
mnemo status
```

Report the version, db path, node counts by type, source count, and daemon
status (running / stopped / stale PID).
